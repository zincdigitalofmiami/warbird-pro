# Warbird Pro — Plan Update Log

Historical record of all plan changes. Extracted from the active plan on 2026-03-31.

---

- 2026-04-01: **Phase 4 final verification PASS. Full migration complete.** Local: mes_15m 144,540 | mes_1h 36,321 | mes_1d 1,919 | cross_asset_15m 20,553 (HG only) | cross_asset_1h 178,379 (6 symbols) | cross_asset_1d 11,098 (6 symbols) | econ_yields_1d 21,839 | econ_rates_1d 3,914 | econ_fx_1d 4,674 | econ_inflation_1d 5,547 | econ_vol_1d 6,309 | econ_indexes_1d 6,823 | econ_labor_1d 943 | econ_activity_1d 623 | econ_money_1d 400 | econ_commodities_1d 3,131. No pre-2020 leak. Cloud: 13 training tables = 0, live tables intact. Punch list: (1) retry PCE + JTSJOL (FRED 500, transient); (2) investigate BOPBCA (returned empty — quarterly BEA series); (3) migration needed: `BAMLHYH0A0HYM2EY` → `BAMLH0A0HYM2OAS` in series_catalog (wrong FRED ID). Cross_asset_15m: NQ/RTY/CL/6E/6J not yet backfilled to 15m — only HG from this session.

- 2026-04-01: **Local DB migration complete + cloud cleanup done.** Cloud Supabase stripped to dashboard-only: 17 training-only tables truncated (mes_1h, mes_4h, mes_1d, cross_asset_15m, cross_asset_1d, all 10 econ_*_1d tables, executive_orders_1d, geopolitical_risk_1d). Live tables intact (mes_15m 52,646 rows, cross_asset_1h 167,361, econ_calendar 10,550). Local Supabase (port 54322) now holds full training warehouse from 2020-01-01: mes_15m 144,540 rows, mes_1h 36,321, mes_1d 1,919, cross_asset_1h 166,517 (5 symbols: NQ/RTY/CL/6E/6J), cross_asset_1d 9,473, econ_yields_1d 7,685 (5 series), econ_rates_1d 3,783 (DFF/SOFR), econ_fx_1d 4,599 (3 series), econ_inflation_1d 4,830 (6 series), econ_vol_1d 6,213 (4 series), econ_indexes_1d 5,859 (5 series). Source: rabid_raccoon → COPY via CSV. Pre-2020 leak check: clean. 57 catalog series still empty (no raccoon source) — require FRED API backfill separately. HG copper cross-asset requires Databento backfill separately.

- 2026-04-01: **Training data floor narrowed to 2020-01-01 (5-year window).** Previous floor of 2018-01-01 (set 2026-03-30) narrowed to 2020-01-01 per Kirk directive. Covers COVID crash, recovery, inflation cycle, and rate hike cycle. All MD docs, plan, model spec, agents, and memory updated. Migration 046 adds 5 FRED inflation-expectations series to `series_catalog` (EXPINF1YR/5YR/10YR/30YR + T5YIFR) as free-tier replacements for the paid Massive Economy API `MASSIVE_IE_*` series. Local Supabase schema-only (no time-series data loaded yet).

- 2026-03-31: **Checkpoint 1 locked; phases 5-7 paused pending broader contract audit.** `supabase/migrations/20260331000045_drop_legacy_warbird_tables.sql` was restored from the recovered Codex session log, replay-verified locally via `supabase db reset`, and now aligns local/remote migration ledgers through `20260331000045`. The 9 dropped legacy objects are absent after replay, canonical admin/candidate surfaces still exist, and `get_admin_table_coverage()` matches the recovered canonical version. This does **not** open Checkpoint 2. A larger issue surfaced: the active plan already requires point-in-time setup truth, realized path truth, published signal lineage, and a separate explanatory/research layer. Current admin-page assumptions, schema/table assumptions, and action/event recording assumptions are not design-locked yet. No writer/admin/schema/training implementation may proceed until that plan-grounded architecture audit is completed.

- 2026-03-31: **Fib engine hardening complete (commit `4a25806`).** Three changes to `indicators/v7-warbird-institutional.pine`: (1) Direction logic replaced midpoint-hysteresis with ZigZag swing-sequence — `fibBull` now only changes on confirmed pivots via `zzSwingDir`, eliminating spurious direction flips during consolidation near the midpoint. All 13 downstream `fibBull` consumers verified. (2) HTF directional agreement gate — 1H/4H `request.security()` tuples expanded from 2-value `[high, low]` to 4-value `[high, low, close, ema(close, 21)]` with zero extra security calls (still 11/40). Entry triggers gated on `htfDirAgrees = (fibBull == htf1hBull) and (fibBull == htf4hBull)` using HTF-native values only. Gate is alert-only; trade state machine unchanged (pre-existing architecture split). (3) Exhaustion diamond visual added via `label.new()` with `label.style_none` and `"◆"` text, bar-close gated, one-shot, direction-aware placement. Zero plot budget cost (63/64 unchanged). Audit confirmed anchor quality, left/right bar space, anchor-span visual gap, and waypoint lines (1.382/1.50/1.786) were already correct — no changes needed. Dead `fibMidpoint` variable removed. Misleading HTF comment cleaned. Forensic review passed with P2 (htfDirAgrees alert-only gate = pre-existing split) and two P3 stale-comment nits (header HTF tuple shape, no-repaint audit date). All gates: pine-facade 0 errors, pine-lint 0 errors (2 pre-existing warnings W9/W6), check-contamination pass, npm build exit 0. Pine validation report: clean across all 10 categories.

- 2026-03-31: **Database truth reconciliation checkpoint.** Read-only audit against both local and cloud DBs showed shared migration-ledger drift: both `supabase_migrations.schema_migrations` tables contain phantom version `20260331000045` even though the repo has only 43 migration files and tops out at `20260331000043`. The legacy warbird operational tables expected by `lib/warbird/queries.ts` and the `/api/warbird/*` routes do not exist on either DB, while the live dashboard client still calls `/api/warbird/dashboard`. Canonical 037/038 surfaces exist on both DBs but are empty; `executive_orders_1d` exists on both but is populated only on cloud. `detect-setups` and `score-trades` remain unscheduled schema-stale writer-port references. `scripts/warbird/build-warbird-dataset.ts` remains the current local extractor target, but it is contract-dirty: legacy `2024-01-01` floor, stale `news_signals` / `trump_effect_1d` / `warbird_setups` reads, unordered setup-history sampling, and non-deterministic `Date.now()` weighting. This gates phases 5-7 until backend/admin/training truth is reconciled, but does not reorder Pine step 4.

- 2026-03-31: **Warbird runtime guard checkpoint.** Added a shared runtime guard for the stale legacy Warbird reader path. `/api/warbird/dashboard`, `/api/warbird/signal`, and `/api/warbird/history` now probe the live Supabase response shape for the legacy reader objects with a service-role client before touching `lib/warbird/queries.ts`. When the objects are absent, the routes return `200` with an explicit `runtime` degradation payload and empty Warbird state instead of throwing `500`s. `DashboardLiveClient.tsx` now shows that degraded mode visibly, including the guard reason, check time, and missing objects. This is containment only, not reader migration.

- 2026-03-31: **MES minute-efficiency checkpoint.** Patched `supabase/functions/mes-1m/index.ts` so incremental ingestion is keyed to the last fully closed 1m boundary rather than raw wall-clock drift, and current in-progress 1m bars are filtered out before `mes_1m` persistence. This removes the alternating `SUCCESS` / `SKIPPED no_gap` churn caused by writing the partial current minute and then finding no real closed-minute gap on the next cron tick. Validation: `npm run build` passed. Deno CLI was not available in the shell for a direct `deno check`.

- 2026-03-31: **Local Supabase setup + full migration reconciliation + news/legacy cleanup.** Docker Desktop + local Supabase running (external drive). 43 migrations replay clean via `db reset`. Migration ledger fully reconciled — 4 never-applied migrations found and fixed (018/031/033/036), 2 partials patched (039/040). Canonical warbird tables (037+038) applied to production — 13 tables + 8 views live but empty. News infrastructure removed (042): finnhub tables, matview, types, crons, Edge Function, Python scripts. trump_effect renamed to executive_orders (043). 14 legacy tables dropped. Security advisor: 0 code warnings. Pine v7 CME Globex intermarket pivot completed (NQ/RTY/CL/HG/6E/6J). Plan cleaned 2963→948 lines. Python Pyright warnings fixed.


- 2026-03-31: **Migration ledger reconciliation.** Full audit of migrations 018-040 against live DB. Found 4 never-applied (018, 031, 033, 036), 2 partially applied (039, 040). Stamped 15 confirmed-applied into ledger, ran 4 missing migrations, patched 2 partials. Security advisor pass: 0 ERRORs. Ledger now reconciled through 040. Only 037+038 (canonical warbird tables) remain intentionally unapplied. `supabase db push` is now safe. Migration discipline rule added to AGENTS.md.
- 2026-03-31: **v7 CME Globex intermarket pivot.** NQ/RTY/CL/HG/6E/6J replace TICK/VOLD/VVIX/VIX/VIX3M/HYG in Pine indicator. Leadership/Risk-appetite/Macro-FX/Exec regime groups. 6J inverted at state boundary. 60 plot + 3 alert = 63/64. Commit `6f3e7a6`.
- 2026-03-31: **Dashboard symbol bar + hourly cross-asset crons.** CorrelationsRow shows HG/NQ/6E/CL tiles from `cross_asset_1h` (green/red = MES-aligned hourly move). Cross-asset crons switched from nightly to hourly (4 shards at :05-:08, Sun-Fri). Chart height locked to 80vh. Migration 039 creates `cross_asset_15m` (RLS enabled). Migration 040 inserts HG into symbols + reschedules crons. Backfill scripts ready. Plan runtime section rewritten to match actual Edge Function cron state. All docs updated for `.c.0` continuous contracts (no manual roll logic).
- 2026-03-31: **Intermarket pivot to CME Globex.** NYSE/CBOE data (TICK, VOLD, VVIX, VIX/VIX3M, HYG, SKEW, ADD) is NOT available from Databento GLBX.MDP3. Replaced v7a flow basket with CME Globex futures: NQ, RTY, CL, HG, 6E, 6J -- all available at 15m from Databento ($0 OHLCV on Standard plan). ES chart-native vol (ATR ratio, range expansion, efficiency, VWAP) fills the VIX gap. Daily context: VIX (FRED `VIXCLS`), SKEW (`CBOE:SKEW`), NYSE A/D (`USI:ADD`) -- daily-only, NOT gate members. Intermarket feature table, ML exports, and request.security() inventory updated throughout plan.
- 2026-03-30: **Kirk chart reading teaching session -- foundational methodology locked.** Structure before indicators, Fibonacci universality (to-the-tick MTF precision), volume as force at fib levels, post-sweep entry methodology, S/R flip as AG feature, training floor previously extended to 2018-01-01 (later narrowed to 2020-01-01), fractal timeframe alignment confirmed. Key AG feature implications: fib proximity + MTF S/R proximity + pivot proximity converging near zero = highest probability zones.
- 2026-03-30: v7 indicator verified and pushed (commit `ffb26f8`). AG labels fixed, 9 collinear plots removed, cooldown 4->8. Budget: 52 plots + 3 alertconditions = 55/64 (9 headroom). All 4 gates passed.
- 2026-03-29: Admin candidate table staple columns locked: Dir, Target, TP1 Hit, TP2 Hit, SL Hit, Status (non-negotiable). TradingView -> Dashboard webhook architecture added. Plan deep cleanup (-600 lines). Pine output budget fixed to 64/64 exact cap (8 alerts cut, 2 plot exports cut, pine-lint bug fixed). Fib visual gap identified (anchor span + waypoint lines). Outcome contract reset: `TP2_HIT`/`TP1_ONLY`/`STOPPED`/`REVERSAL`/`OPEN`.
- 2026-03-28: Pine type-safety hardening (80+ explicit type annotations). Locked corrected mental model (AG tunes settings via SHAP, not anchor placement; entry is the crown jewel). Hierarchy-first architecture checkpoint locked (Generator/Selector/Diagnostician split). Schema/admin reconciliation completed. Legacy `prob_hit_*` aliases removed. Restart-safe handoff saved.
- 2026-03-27: Architecture reset after live repo audit -- canonical flow locked as `fib_engine_snapshot -> candidate -> outcome -> decision -> signal`. Retention-floor trim migration applied (2024-01-01 forward).
- 2026-03-26: Supabase Edge cron cutover guardrails locked. Strategy path retired (indicator-only). Pine budget work (exhaustion checkpoint, 64-cap discovery, news schema, backend checkpoint). Cloud-first architecture locked. MES minute schedule cut to Supabase-owned. Cost/abuse hardening. Newsfilter locked as primary news source. See `docs/plans/archive/2026-03-31-plan-cleanup-removed-content.md` for full March 26 detail.
- 2026-03-24: Phase 2 completed (5 checkpoints). Phase 3 strategy created + retired. Massive inflation-expectations validated + backfilled. Newsfilter/Finnhub raw-news contracts added. Provider-agnostic macro requirement locked. Three standalone harnesses admitted then retired 2026-03-28 (replaced by TA core pack). See archive for full checkpoint detail.
- 2026-03-23: Rewrote the active plan from the live audit: locked current Supabase pg_cron reality vs target Supabase-owned runtime, exact Databento/FRED/news scope, raw Google News intake contract, cloud realtime/dashboard surfaces, and the 2-consecutive-PT1-miss rollback rule.
- 2026-03-23: Absorbed the March 23 execution delta into an explicit phase-order lock and recorded a Phase 2 kickoff audit against `indicators/v6-warbird-complete.pine`.
- 2026-03-23: Added audited Phase 4 operations requirements for the local PostgreSQL training warehouse, cloud publish-up tables, and packet / run lifecycle after verifying the linked Supabase project and live Supabase schema.
- 2026-03-23: Added phased execution guide for canonical 15m contract, cloud-first production plus local training publish-up flow, third-party Pine admission gate, BigBeluga pivot replacement, required harness-module admission for BigBeluga plus LuxAlgo Market Structure Break & OB Probability Toolkit, and later-phase candidate evaluation rules.
- 2026-03-22: Added dependency-security remediation checkpoint order (plan updates first, then implementation). Scope includes Next.js and transitive lockfile remediation plus `xlsx` ingestion-surface removal/replacement.
- 2026-03-20: Converted the active plan into a single-indicator plan.
- 2026-03-20: Archived older plan docs and removed them from the active path.
- 2026-03-20: AG model concept — fib continuation probability engine with TP1/TP2 targets (1.236/1.618 extensions), re-entry signals, full macro/economic training context, and Pine config packet output.
- 2026-03-20: Added "AG Models Pine's Configuration Space" — AG output must be Pine-native (exact input values, thresholds, weights, gates, module decisions).
- 2026-03-20: Added Forensic Review of current script — 8 high-risk problems to fix before AG training.
- 2026-03-20: Restructured plan around Canonical Goal / Canonical Outputs / Canonical Standards / Locked v1 Mechanisms. The product goal and chart-output surface are canonical. The v1 build path is now locked.
